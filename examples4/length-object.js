let name = {
    first: 'trilok',
    last: 'singh'
}
// using Object.keys()
console.log(Object.keys(name).length)
// using Object.values()
console.log(Object.values(name).length)

// using Object.getOwnPropertyNames()
console.log(Object.getOwnPropertyNames(name).length)

